<odoo>
    <template id="sale_order_portal_template" inherit_id="sale.sale_order_portal_template">
        <xpath expr="//div[hasclass('o_portal_sale_sidebar')]/t[@id='sale_order_portal_sidebar']/t[@t-set='entries']//div[@id='sale_order_sidebar_button']/a[1]" position="before">
            <a t-if="sale_order._has_to_be_signed()" role="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#modal_bankid_ssn" href="#">
                <i class="fa fa-pencil"/><t t-if="sale_order._has_to_be_paid()"> Sign with BankID</t><t t-else=""> Sign with BankID</t>
            </a>
        </xpath>

        <xpath expr="//div[hasclass('o_portal_sale_sidebar')]/div[@id='quote_content']" position="inside">
            <!-- modal relative to the actions bankid ssn -->
            <div role="dialog" class="modal fade" id="modal_bankid_ssn" name="sale_order_modal_bankid_ssn">
                <div class="modal-dialog" t-if="sale_order._has_to_be_signed()">
                    <form id="bankid_sign" method="POST" t-att-data-order-id="sale_order.id" t-att-data-token="sale_order.access_token" class="js_accept_json modal-content js_bankid_ssn_submit_form">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <header class="modal-header">
                            <h4 class="modal-title">Sign with BankID</h4>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"/>
                        </header>
                        <main class="modal-body" id="sign-dialog">
                            <span>
                                By entering your personal number, you confirm acceptance on behalf of
                                <b t-field="sale_order.partner_id.commercial_partner_id"/>
                                for the <b data-id="total_amount" t-field="sale_order.amount_total"/> quote.
                            </span>
                            <b t-if="sale_order.payment_term_id" t-field="sale_order.payment_term_id.note"/>

                            <div class="o_web_sign_name_group">
                                <label class="col-form-label" for="ssn">Personal Number</label>
                                <input type="text" name="ssn" class="o_web_sign_name_input form-control" placeholder="Type your SSN to sign" required=""/>
                            </div>

                        </main>
                        <footer class="modal-footer">
                            <button type="button" t-att-id="sale_order.id" class="btn btn-primary bankid_sign_init">
                                <i class="fa fa-pencil"/> Sign
                            </button>
                            <button type="button" class="btn btn-danger" data-bs-dismiss="modal">
                                Cancel
                            </button>
                        </footer>
                    </form>
                </div>
            </div>
        </xpath>


    </template>
</odoo>